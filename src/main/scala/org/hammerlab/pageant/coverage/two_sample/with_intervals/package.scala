package org.hammerlab.pageant.coverage.two_sample

import org.hammerlab.pageant.coverage.two_sample

package object with_intervals {
  type PDF = two_sample.PDF[Counts]
  type CDF = two_sample.CDF[Counts]
}
